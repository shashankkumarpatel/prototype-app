import styled from '@emotion/styled';
import { Modal } from '@spglobal/react-components';

export const ModalStyled = styled(Modal)`
  display: flex;
  flex-direction: column;
  height: calc(100% - 80px);
`;
